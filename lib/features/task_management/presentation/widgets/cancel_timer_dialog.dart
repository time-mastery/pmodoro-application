import 'package:flutter/material.dart';

class CancelTimerDialog extends StatelessWidget {
  const CancelTimerDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
